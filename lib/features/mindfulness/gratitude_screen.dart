import 'package:flutter/material.dart';

class GratitudeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gratitude Screen'),
      ),
      body: Center(
        child: Text('Gratitude Screen'),
      ),
    );
  }
}