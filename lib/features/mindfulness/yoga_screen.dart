import 'package:flutter/material.dart';

class YogaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Yoga Screen'),
      ),
      body: Center(
        child: Text('Yoga Screen'),
      ),
    );
  }
}